import { WindowManager } from './window-manager';

export class WindowContextMenu {
    public constructor(_host: HTMLElement, _windowManager: WindowManager) {
        // TODO: Implement context menu interactions for the new window system.
    }

    public dispose(): void {
        // no-op for now
    }
}

